# Google Sheets Configuration
GOOGLE_SERVICE_ACCOUNT_JSON_PATH=credentials/service_account.json
GOOGLE_SHEETS_KEY=your_google_sheets_key_here

# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# NewsData.io API (for --auto mode)
NEWSDATA_API_KEY=your_newsdata_api_key_here

# Pexels API (for stock photos)
PEXELS_API_KEY=your_pexels_api_key_here

# AI Horde API (for image generation) - DEPRECATED, use Pexels instead
# AI_HORDE_API_KEY=your_ai_horde_api_key_here
# Facebook (examples only — DO NOT commit real tokens)
# 1) Get a long-lived USER token with scopes: pages_manage_posts, pages_read_engagement, pages_show_list, publish_video
# 2) Exchange it for a PAGE token via /me/accounts and use that PAGE token as FB_PAGE_TOKEN in Secret Manager
# Example helper (run in Cloud Shell, replace YOUR_LONG_LIVED_USER_TOKEN):
# curl -s "https://graph.facebook.com/v19.0/me/accounts?access_token=YOUR_LONG_LIVED_USER_TOKEN" | sed 's/},{/},\n{/'